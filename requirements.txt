# Core
anthropic>=0.40.0
openai>=1.60.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Knowledge Base
lancedb>=0.17.0          # Vector DB — pure Python, ANN index, Python 3.14+ OK
pymupdf>=1.24.0          # PDF parsing
python-docx>=1.1.0      # DOCX parsing
beautifulsoup4>=4.12.0  # HTML parsing
lxml>=5.0.0
tiktoken>=0.7.0         # Token counting for chunking

# Memory
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# Web/API
fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.28.0

# MCP Server
fastmcp>=2.0.0

# Browser Automation (reuse existing browser-harness)
# browser-harness is on $PATH

# SEO API Integrations
PyJWT>=2.8.0            # GSC service account auth
google-auth>=2.30.0     # Google API auth (optional)

# Logging
loguru>=0.7.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Utilities
rich>=13.0.0            # CLI formatting
click>=8.0.0            # CLI framework
apscheduler>=3.10.0     # Scheduled tasks
